version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    # env_file:
    #   - ./.env
    environment:
      - DATABASE_HOSTNAME=postgres
      - DATABASE_PORT=5432
      - DATABASE_PASSWORD=Ackermann@9
      - DATABASE_NAME=fastapi
      - DATABASE_USERNAME=postgres
      - SECRET_KEY=06034343d3e37fc026cdb9b91ed2b56a7456f774c420c1a6e70ecff9194645f92710
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30  
  postgres:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Ackermann@9
      - POSTGRES_DB=fastapi
    volumes:
      - postgres_db:var/lib/postgresql/data

volumes:
   postgres_data:
      
     